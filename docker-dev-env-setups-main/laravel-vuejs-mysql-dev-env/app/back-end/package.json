{
    "private": true,
    "scripts": {
        "dev": "vite",
        "serve": "php artisan serve --port=8080 --host=0.0.0.0",
        "build": "vite build"
    },
    "devDependencies": {
        "axios": "^1.1.2",
        "laravel-vite-plugin": "^0.7.2",
        "vite": "^4.0.0"
    }
}
